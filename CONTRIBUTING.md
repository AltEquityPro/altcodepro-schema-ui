# Contributing to AltCodePro Schema UI

We’re excited that you want to contribute 🎉. This package powers **[AltCodePro](https://altcode.pro)** project generation clients, and contributions help make schema-driven UI more powerful and reliable.

---

## 📋 Code of Conduct

Please follow the [Contributor Covenant](https://www.contributor-covenant.org/) guidelines.
Be respectful, inclusive, and constructive in discussions and code reviews.

---

## 🛠 Local Development Setup

1. **Fork and clone the repo**

   ```bash
   git clone https://github.com/<your-org>/<repo>.git
   cd <repo>
   ```

2. **Install dependencies**

   ```bash
   npm install
   ```

3. **Build the package**

   ```bash
   npm run build
   ```

4. **Run tests**

   ```bash
   npm test
   ```

5. **Link locally (optional)**
   To test the library in another project:

   ```bash
   npm link
   cd ../your-app
   npm link @altcodepro/schema-ui
   ```

---

## 📦 Project Structure

* `src/types/` → TypeScript schema definitions (`UIProject`, `UIScreenDef`, `UIElement`, etc.)
* `src/schema/` → Core runtime (`StateContext`, `Actions`, etc.)
* `src/components/` → UI components (`FileUpload`, `FileUploadRenderer`, etc.)
* `dist/` → Compiled output (do not edit, auto-generated by `tsc`)
* `README.md` → Documentation
* `LICENSE` → MIT License

---

## 🧪 Testing

We use **Jest** for testing.

Run all tests:

```bash
npm test
```

Generate coverage:

```bash
npm run test:coverage
```

---

## 🚀 Publishing (Maintainers only)

1. Ensure build passes:

   ```bash
   npm run build
   ```

2. Bump version in `package.json` (semver: major/minor/patch):

   ```bash
   npm version patch
   ```

3. Publish to npm:

   ```bash
   npm publish --access public
   ```

---

## ✨ How to Contribute

* **Bug reports:** Open a GitHub Issue with clear steps to reproduce.
* **Feature requests:** Open an Issue tagged `enhancement`.
* **Code contributions:** Submit a Pull Request (PR).

  * Use **TypeScript** and follow existing code style.
  * Add/update tests when changing functionality.
  * Update docs/examples if needed.

---

## 📢 Communication

* Website: [altcode.pro](https://altcode.pro)
* Issues: GitHub Issue Tracker
* Support: Reach out via project Slack/Discord (invite available on request)

---

✅ Save this as `CONTRIBUTING.md` in your repo root.
